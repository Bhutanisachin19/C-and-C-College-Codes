#include <stdio.h>
int main()
{
    int a,b,sum;
    char c;
    scanf("%d%c%d",&a,&c,&b);

    if(c=='+')
        {sum=a+b;
            printf("%d",sum);
        }
    /*else if(c=="-")
        return sum=a-b;
    else if(c=="*")
        return sum=a*b;
    else if(c=="/")
        return sum =a/b;
    else
        printf("wrong input");*/





}
